apiVersion: v1
kind: Service
metadata:
    name: "myakscluster-09ca"
    labels:
        app: "myakscluster-09ca"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "myakscluster-09ca"